server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
   #��ʾ�Լ���Eureka Server
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/